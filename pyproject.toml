[project]
name = "ipcalc"
version = "0.1.0"
description = ""
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ipcalc = "ipcalc.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ipcalc"]
